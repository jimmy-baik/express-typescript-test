<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>피드 목록</title>
    <link href="https://fonts.googleapis.com/css2?family=Charter:ital,wght@0,400;0,700;1,400&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/header-styles.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: #ffffff;
            color: #000000;
            line-height: 1.6;
        }

        .cta-primary {
            padding: 16px 32px;
            background-color: #292929;
            color: #ffffff;
            text-decoration: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            transition: background-color 0.2s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .cta-primary:hover {
            background-color: #000000;
        }

        /* Main Content */
        .main-content {
            margin-top: 60px;
            padding: 0 20px;
        }

        .content-container {
            max-width: 1192px;
            margin: 0 auto;
            padding: 40px 0;
        }

        /* Feed Cards */
        .feeds-list {
            display: flex;
            flex-direction: column;
            gap: 32px;
        }

        .feed-card {
            background: #ffffff;
            border: 1px solid #e6e6e6;
            border-radius: 8px;
            padding: 32px;
            transition: box-shadow 0.2s ease;
        }

        .feed-card:hover {
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .feed-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 24px;
        }

        .feed-title-section {
            flex: 1;
        }

        .feed-title {
            font-size: 24px;
            font-weight: 700;
            color: #000000;
            margin-bottom: 8px;
        }

        .feed-meta {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 14px;
            color: #6b6b6b;
        }

        .people-icon {
            width: 16px;
            height: 16px;
            color: #6b6b6b;
        }

        .member-count {
            font-weight: 500;
        }

        .feed-posts {
            display: flex;
            flex-direction: column;
            gap: 24px;
            margin-bottom: 24px;
        }

        .post-item {
            display: flex;
            align-items: flex-start;
            gap: 16px;
            padding: 16px;
            background: #f9f9f9;
            border-radius: 8px;
            border: 1px solid #f0f0f0;
        }

        .post-content {
            flex: 1;
            min-width: 0;
        }

        .post-author-info {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 8px;
        }

        .author-avatar {
            width: 20px;
            height: 20px;
            border-radius: 50%;
            background: #e6e6e6;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 10px;
            font-weight: 600;
            color: #6b6b6b;
        }

        .author-name {
            font-size: 13px;
            color: #292929;
            font-weight: 500;
        }

        .post-title {
            margin-bottom: 8px;
        }

        .post-title a {
            font-size: 18px;
            font-weight: 600;
            color: #000000;
            text-decoration: none;
            line-height: 1.3;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .post-title a:hover {
            color: #6b6b6b;
        }

        .post-subtitle {
            font-size: 14px;
            color: #6b6b6b;
            line-height: 1.4;
            margin-bottom: 8px;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .post-meta {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 12px;
            color: #6b6b6b;
        }

        .view-more-btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #292929;
            color: #ffffff;
            text-decoration: none;
            border-radius: 6px;
            font-size: 14px;
            font-weight: 500;
            transition: background-color 0.2s ease;
            text-align: center;
        }

        .view-more-btn:hover {
            background-color: #000000;
        }

        /* Create New Feed Section */
        .create-feed-section {
            margin-top: 48px;
            padding: 32px;
            background: #f9f9f9;
            border: 2px dashed #e6e6e6;
            border-radius: 8px;
            text-align: center;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .create-feed-section:hover {
            background: #f5f5f5;
            border-color: #292929;
        }

        .create-feed-link {
            display: block;
            text-decoration: none;
            color: inherit;
        }

        .create-feed-icon {
            width: 48px;
            height: 48px;
            margin: 0 auto 16px;
            color: #6b6b6b;
        }

        .create-feed-text {
            font-size: 18px;
            font-weight: 600;
            color: #292929;
            margin-bottom: 8px;
        }

        .create-feed-subtext {
            font-size: 14px;
            color: #6b6b6b;
        }

        .no-feeds {
            text-align: center;
            padding: 80px 20px;
            color: #6b6b6b;
        }

        .no-feeds-title {
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 16px;
            color: #292929;
        }

        .no-feeds-text {
            font-size: 16px;
            margin-bottom: 32px;
        }

        /* Empty state for posts */
        .no-posts {
            padding: 24px;
            text-align: center;
            color: #6b6b6b;
            font-size: 14px;
            background: #f9f9f9;
            border-radius: 8px;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .content-container {
                padding: 24px 0;
            }

            .feed-card {
                padding: 24px;
            }

            .feed-title {
                font-size: 20px;
            }

            .post-item {
                padding: 12px;
            }

            .create-feed-section {
                padding: 24px;
            }
        }
    </style>
</head>
<body>

    <%- include('partials/header.ejs') %>

    <!-- 메인 컨텐츠 -->
    <main class="main-content">
        <div class="content-container">
            <% if (feeds && feeds.length > 0) { %>
                <div class="feeds-list">
                    <% feeds.forEach(function(feed) { %>
                        <div class="feed-card">
                            <div class="feed-header">
                                <div class="feed-title-section">
                                    <h2 class="feed-title"><%= feed.title %></h2>
                                    <div class="feed-meta">
                                        <svg class="people-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                                            <circle cx="9" cy="7" r="4"></circle>
                                            <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                                            <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
                                        </svg>
                                        <span class="member-count"><%= feed.memberUserIds ? feed.memberUserIds.length : 0 %>명</span>
                                    </div>
                                </div>
                            </div>

                            <div class="feed-posts">
                                <% if (feed.posts && feed.posts.length > 0) { %>
                                    <% feed.posts.slice(0, 3).forEach(function(post) { %>
                                        <div class="post-item">
                                            <div class="post-content">
                                                <div class="post-author-info">
                                                    <span class="author-avatar">U</span>
                                                    <span class="author-name">사용자</span>
                                                </div>
                                                <% if (post.title) { %>
                                                    <h3 class="post-title">
                                                        <a href="<%= post.originalUrl || '#' %>" target="_blank" rel="noopener noreferrer"><%= post.title %></a>
                                                    </h3>
                                                <% } %>
                                                <% if (post.generatedSummary) { %>
                                                    <div class="post-subtitle"><%= post.generatedSummary %></div>
                                                <% } else if (post.textContent) { %>
                                                    <div class="post-subtitle"><%= post.textContent.substring(0, 150) %><%= post.textContent.length > 150 ? '...' : '' %></div>
                                                <% } %>
                                                <div class="post-meta">
                                                    <span><%= new Date(post.submittedAt || post.createdAt).toLocaleDateString('ko-KR', { month: 'short', day: 'numeric' }) %></span>
                                                </div>
                                            </div>
                                        </div>
                                    <% }); %>
                                <% } else { %>
                                    <div class="no-posts">아직 게시글이 없습니다.</div>
                                <% } %>
                            </div>

                            <a href="/feeds/<%= feed.slug %>" class="view-more-btn">더 보기</a>
                        </div>
                    <% }); %>
                </div>
                <!-- 새 피드 만들기 -->
                <div class="create-feed-section">
                    <a href="/feeds/new" class="create-feed-link">
                        <svg class="create-feed-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <line x1="12" y1="5" x2="12" y2="19"></line>
                            <line x1="5" y1="12" x2="19" y2="12"></line>
                        </svg>
                        <div class="create-feed-text">피드 추가하기</div>
                        <div class="create-feed-subtext">새로운 주제의 피드를 만들어 보세요.</div>
                    </a>
                </div>
            <% } else { %>
                <div class="no-feeds">
                    <h2 class="no-feeds-title">피드가 없습니다</h2>
                    <p class="no-feeds-text">새 피드를 만들어 시작해보세요.</p>
                    <a href="/feeds/new" class="cta-primary">
                        <span>+ 피드 추가하기</span>
                    </a>
                </div>
            <% } %>
        </div>
    </main>
</body>
</html>

