
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Charter:ital,wght@0,400;0,700;1,400&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/header-styles.css">
    <link rel="stylesheet" href="/css/posts-styles.css">
</head>
<body>

    <%- include('partials/header.ejs') %>

    <!-- 컨텐츠 추가하기 버튼 -->
    <% if (typeof feedSlug !== 'undefined') { %>
        <a href="/feeds/<%= feedSlug %>/new-url" class="add-content-btn">
            <svg class="add-content-btn-icon" viewBox="0 0 24 24">
                <line x1="12" y1="5" x2="12" y2="19"></line>
                <line x1="5" y1="12" x2="19" y2="12"></line>
            </svg>
            컨텐츠 추가하기
        </a>
    <% } %>

    <!-- 메인 컨텐츠 -->
    <main class="main-content">
        <div class="content-container">
            <% if (posts && posts.length > 0) { %>
                <div class="posts-list">
                    <% posts.forEach(function(post) { %>
                        <article class="post-item">
                            <div class="post-content">
                                <!-- <div class="post-author-info">
                                    <span class="author-name"><%= post.ownerUserId %></span>
                                </div> -->
                                <h2 class="post-title">
                                    <a href="<%= post.originalUrl %>" data-post-id="<%= post.postId %>"><%= post.title %></a>
                                </h2>
                                <div class="post-subtitle">
                                    <%= post.textContent ? post.textContent.substring(0, 150) + '...' : '설명 없음' %>
                                </div>
                                <div class="post-meta">
                                    <div class="meta-item">
                                        <span><%= new Date(post.submittedAt).toLocaleDateString('ko-KR', { month: 'short', day: 'numeric' }) %></span>
                                    </div>
                                </div>
                                <% if (post.generatedSummary) { %>
                                    <div class="accordion-section" data-post-id="<%= post.postId %>">
                                        <button class="accordion-toggle" type="button">
                                            <span class="accordion-label">✶.˚ AI 요약</span>
                                            <svg class="accordion-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                                <polyline points="6 9 12 15 18 9"></polyline>
                                            </svg>
                                        </button>
                                        <div class="accordion-content">
                                            <div class="accordion-summary"><%= post.generatedSummary %></div>
                                            <a href="<%= post.originalUrl %>" class="view-full-btn" target="_blank" rel="noopener noreferrer">더 읽기</a>
                                        </div>
                                    </div>
                                <% } %>

                            </div>
                            <div class="post-actions">
                                <svg class="heart-icon <%= (userLikedPosts && userLikedPosts.includes(post.postId)) ? 'liked' : '' %>" 
                                     viewBox="0 0 24 24" 
                                     fill="<%= (userLikedPosts && userLikedPosts.includes(post.postId)) ? 'currentColor' : 'none' %>" 
                                     stroke="currentColor" 
                                     stroke-width="2"
                                     data-post-id="<%= post.postId %>">
                                    <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
                                </svg>
                                <!-- <svg class="action-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <circle cx="12" cy="12" r="1"></circle>
                                    <circle cx="19" cy="12" r="1"></circle>
                                    <circle cx="5" cy="12" r="1"></circle>
                                </svg> -->
                            </div>
                        </article>
                    <% }); %>
                </div>
            <% } else { %>
                <div class="posts-list"></div>
            <% } %>

            <!-- 게시글 row 템플릿 -->
            <template id="post-template">
                <article class="post-item">
                    <div class="post-content">
                        <div class="post-author-info">
                            <span class="author-name"></span>
                        </div>
                        <h2 class="post-title">
                            <a href=""></a>
                        </h2>
                        <div class="post-subtitle"></div>
                        <div class="post-meta">
                            <div class="meta-item">
                                <span></span>
                            </div>
                        </div>
                        <div class="accordion-section" data-post-id="">
                            <button class="accordion-toggle" type="button">
                                <span class="accordion-label">✶.˚ AI 요약</span>
                                <svg class="accordion-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <polyline points="6 9 12 15 18 9"></polyline>
                                </svg>
                            </button>
                            <div class="accordion-content">
                                <div class="accordion-summary"></div>
                                <a href="" class="view-full-btn" target="_blank" rel="noopener noreferrer">전체 보기</a>
                            </div>
                        </div>
                    </div>
                    <div class="post-actions">
                        <svg class="heart-icon" 
                             viewBox="0 0 24 24" 
                             fill="none" 
                             stroke="currentColor" 
                             stroke-width="2"
                             data-post-id="">
                            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
                        </svg>
                    </div>
                </article>
            </template>

            <!-- 로딩 템플릿 -->
            <template id="loading-template">
                <div class="loading-indicator">
                    <div class="spinner"></div>
                    <p>더 많은 게시글을 불러오는 중...</p>
                </div>
            </template>
            
            <% if (!posts || posts.length === 0) { %>
                <div class="no-posts">
                    <p>게시글이 없습니다.</p>
                </div>
            <% } %>
        </div>
        <div id="scroll-target"></div>
    </main>

</body>
<script src="/js/posts.js"></script>
</html>

