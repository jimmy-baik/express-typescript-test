
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Charter:ital,wght@0,400;0,700;1,400&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/header-styles.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: #ffffff;
            color: #000000;
            line-height: 1.6;
        }

        /* Navigation */
        .navigation {
            position: fixed;
            top: 60px;
            left: 0;
            right: 0;
            background: #ffffff;
            border-bottom: 1px solid #e6e6e6;
            z-index: 999;
            padding: 0 20px;
        }

        .nav-content {
            max-width: 1192px;
            margin: 0 auto;
            display: flex;
            gap: 32px;
            padding: 16px 0;
        }

        .nav-tab {
            color: #6b6b6b;
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            padding-bottom: 8px;
            position: relative;
            transition: color 0.2s ease;
        }

        .nav-tab.active {
            color: #000000;
            font-weight: 600;
        }

        .nav-tab.active::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 2px;
            background-color: #292929;
        }

        /* Main Content */
        .main-content {
            margin-top: 40px;
            padding: 0 20px;
        }

        .content-container {
            max-width: 1192px;
            margin: 0 auto;
        }

        .posts-list {
            padding: 40px 0;
        }

        .post-item {
            display: flex;
            align-items: flex-start;
            gap: 24px;
            padding: 32px 0;
            border-bottom: 1px solid #f0f0f0;
        }

        .post-item:last-child {
            border-bottom: none;
        }

        .post-content {
            flex: 1;
            min-width: 0;
        }

        .post-author-info {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 8px;
        }

        .author-avatar {
            width: 20px;
            height: 20px;
            border-radius: 50%;
            background: #e6e6e6;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 10px;
            font-weight: 600;
            color: #6b6b6b;
        }

        .author-name {
            font-size: 13px;
            color: #292929;
            font-weight: 500;
        }

        .verified-badge {
            width: 12px;
            height: 12px;
            color: #1a73e8;
        }

        .post-title {
            margin-bottom: 8px;
        }

        .post-title a {
            font-size: 20px;
            font-weight: 700;
            color: #000000;
            text-decoration: none;
            line-height: 1.3;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .post-title a:hover {
            color: #6b6b6b;
        }

        .post-subtitle {
            font-size: 16px;
            color: #6b6b6b;
            line-height: 1.4;
            margin-bottom: 12px;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .post-meta {
            display: flex;
            align-items: center;
            gap: 16px;
            font-size: 13px;
            color: #6b6b6b;
        }

        .meta-item {
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .meta-icon {
            width: 14px;
            height: 14px;
        }

        .post-thumbnail {
            width: 112px;
            height: 112px;
            border-radius: 8px;
            background: #f0f0f0;
            flex-shrink: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #6b6b6b;
            font-size: 12px;
        }

        .post-thumbnail img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 8px;
        }

        .post-actions {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-left: auto;
        }

        .action-icon {
            width: 16px;
            height: 16px;
            padding: 2px 0;
            color: #6b6b6b;
            cursor: pointer;
            transition: color 0.2s ease;
        }

        .action-icon:hover {
            color: #292929;
        }

        .heart-icon {
            width: 24px;
            height: 24px;
            padding: 4px;
            color: #6b6b6b;
            cursor: pointer;
            transition: all 0.2s ease;
            border-radius: 4px;
        }

        .heart-icon:hover {
            color: #dc3545;
            background-color: #f8d7da;
        }

        .heart-icon.liked {
            color: #dc3545;
            fill: #dc3545;
        }

        .heart-icon.liked:hover {
            color: #b02a37;
            fill: #b02a37;
            background-color: #f5c6cb;
        }

        /* Hidden */
        .hidden-actions {
            display: none;
        }

        .no-posts {
            text-align: center;
            padding: 80px 20px;
            color: #6b6b6b;
        }

        .loading-indicator {
            text-align: center;
            padding: 40px;
            color: #6b6b6b;
        }

        .spinner {
            border: 2px solid #f3f3f3;
            border-top: 2px solid #292929;
            border-radius: 50%;
            width: 24px;
            height: 24px;
            animation: spin 1s linear infinite;
            margin: 0 auto 12px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .error-message {
            text-align: center;
            padding: 20px;
            color: #dc3545;
            background: #f8d7da;
            border: 1px solid #f5c6cb;
            border-radius: 4px;
            margin: 20px 0;
        }

        /* 반응형 대응 */
        @media (max-width: 768px) {
            .nav-content {
                padding: 0 16px;
            }
            
            .main-content {
                padding: 0 16px;
            }
            
            .post-item {
                flex-direction: column;
                gap: 16px;
            }
            
            .post-thumbnail {
                width: 100%;
                height: 200px;
            }
        }
        
    </style>
</head>
<body>

    <%- include('partials/header.ejs') %>

    <!-- 메인 컨텐츠 -->
    <main class="main-content">
        <div class="content-container">
            <% if (posts && posts.length > 0) { %>
                <div class="posts-list">
                    <% posts.forEach(function(post) { %>
                        <article class="post-item">
                            <div class="post-content">
                                <div class="post-author-info">
                                    <span class="author-name"><%= post.createdBy %></span>
                                </div>
                                <h2 class="post-title">
                                    <a href="<%= post.sourceUrl %>" data-post-id="<%= post.id %>"><%= post.title %></a>
                                </h2>
                                <div class="post-subtitle">
                                    <%= post.content ? post.content.substring(0, 150) + '...' : '설명 없음' %>
                                </div>
                                <div class="post-meta">
                                    <div class="meta-item">
                                        <span><%= new Date(post.timestamp).toLocaleDateString('ko-KR', { month: 'short', day: 'numeric' }) %></span>
                                    </div>
                                </div>
                            </div>
                            <div class="post-actions">
                                <svg class="heart-icon <%= (userLikedPosts && userLikedPosts.includes(post.id)) ? 'liked' : '' %>" 
                                     viewBox="0 0 24 24" 
                                     fill="<%= (userLikedPosts && userLikedPosts.includes(post.id)) ? 'currentColor' : 'none' %>" 
                                     stroke="currentColor" 
                                     stroke-width="2"
                                     data-post-id="<%= post.id %>">
                                    <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
                                </svg>
                                <!-- <svg class="action-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <circle cx="12" cy="12" r="1"></circle>
                                    <circle cx="19" cy="12" r="1"></circle>
                                    <circle cx="5" cy="12" r="1"></circle>
                                </svg> -->
                            </div>
                        </article>
                    <% }); %>
                </div>
            <% } else { %>
                <div class="posts-list"></div>
            <% } %>

            <!-- 게시글 row 템플릿 -->
            <template id="post-template">
                <article class="post-item">
                    <div class="post-content">
                        <div class="post-author-info">
                            <span class="author-name"></span>
                        </div>
                        <h2 class="post-title">
                            <a href=""></a>
                        </h2>
                        <div class="post-subtitle"></div>
                        <div class="post-meta">
                            <div class="meta-item">
                                <span></span>
                            </div>
                        </div>
                    </div>
                    <div class="post-actions">
                        <svg class="heart-icon" 
                             viewBox="0 0 24 24" 
                             fill="none" 
                             stroke="currentColor" 
                             stroke-width="2"
                             data-post-id="">
                            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
                        </svg>
                    </div>
                </article>
            </template>

            <!-- 로딩 템플릿 -->
            <template id="loading-template">
                <div class="loading-indicator">
                    <div class="spinner"></div>
                    <p>더 많은 게시글을 불러오는 중...</p>
                </div>
            </template>
            
            <% if (!posts || posts.length === 0) { %>
                <div class="no-posts">
                    <p>게시글이 없습니다.</p>
                </div>
            <% } %>
        </div>
    </main>

</body>
<script src="/js/posts.js"></script>
</html>

